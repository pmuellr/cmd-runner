# port to bind the HTTP server to
port = 8080

# host to bind the HTTP server on
host = "localhost"

[[commands]]
id = 'npm-test'
name = 'npm test'
command = 'npm test'
cwd = "."

[[commands]]
id = 'ls'
name = 'list files'
command = 'ls'
cwd = "."
